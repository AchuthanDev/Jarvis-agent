# JARVIS Roadmap

Incremental development plan. Every phase must leave the platform runnable via
`docker compose up -d`, with tests passing and docs updated.

Legend: ✅ done · ⏳ in progress · ⬜ not started

---

## Phase 0 — Repository Analysis ✅

- [x] Clone `GauravSingh9356/J.A.R.V.I.S`
- [x] Analyse every source file
- [x] `docs/CURRENT_ARCHITECTURE.md`
- [x] `docs/MIGRATION.md`
- [x] `docs/TARGET_ARCHITECTURE.md`
- [x] Preserve original source under `legacy/`

**Acceptance:** analysis docs exist; no platform code written yet.

---

## Phase 1 — Foundation ✅

- [x] Project scaffolding (`pyproject.toml`, config, logging, versioning)
- [x] Database foundation (SQLAlchemy async, Alembic, initial schema)
- [x] FastAPI app + `/api/health/live`, `/api/health/ready`
- [x] Redis for presence/state (justified: device presence + pub/sub + queues)
- [x] Docker Compose: `jarvis-api`, `postgres`, `redis`
- [x] `.env.example`, structured logging, health checks
- [x] Tests: config + health endpoints
- [x] `docker compose up -d` verified + migrations run

**Acceptance:** `docker compose up -d` starts the platform; `/api/health/ready` reports
DB + Redis health; schema migrations apply cleanly. **Verified 2026-08-14:** clean
start from empty volumes, migration autogenerated from models, `alembic check` reports no
drift, 6/6 tests pass, ruff clean.

---

## Phase 2 — AI Chat Brain ✅

- [x] `core/llm` provider abstraction (OpenAI-compatible, Gemini, Groq, Ollama)
- [x] Conversations + message persistence
- [x] Contextual chat endpoint with streaming
- [x] Basic dashboard chat UI
- [x] Tests with mocked LLM

**Acceptance:** a normal contextual conversation with JARVIS works. **Verified 2026-08-14:**
non-streaming chat, contextual follow-up, and SSE streaming all confirmed live against
Ollama (`llama3.2:1b`); conversations persist and appear in the history API; 29/29 tests
pass, ruff clean, `alembic check` reports no drift; dashboard served at `/`.

---

## Phase 3 — Tool Calling ✅

- [x] Tool registry + typed `Tool` metadata (name, description, parameters, risk, timeout)
- [x] Agent loop: plan → select → validate → execute → observe → respond
- [x] First tools: `web.search`, `current_time`, `server.system_status`,
      `memory.remember`, `memory.recall`
- [x] Permission check between LLM output and execution
- [x] Tool-call audit logging
- [x] Tests

**Acceptance:** JARVIS decides and invokes tools, not hardcoded phrase matching.
**Verified 2026-08-14:** agent loop round-trips live against Ollama (`qwen2.5:0.5b`) —
`current_time` and `memory.remember` executed end-to-end with audit + `tool_calls` +
`memories` rows persisted; 68/68 tests pass, ruff clean, `alembic check` reports no drift.

---

## Phase 4 — Windows Agent ⬜

- [ ] `device_agents/windows` Python companion
- [ ] Secure WebSocket connect, device registration, heartbeat, reconnect
- [ ] Actions: open URL, open app, notification, system info
- [ ] `docs/DEVICE_PROTOCOL.md`, `docs/WINDOWS_AGENT.md`
- [ ] Protocol + permission tests

**Acceptance:** "Open Google on my laptop" opens Google on the registered laptop.

---

## Phase 5 — Voice ⬜

- [ ] Wake word (openWakeWord)
- [ ] VAD
- [ ] STT provider interface (faster-whisper first)
- [ ] TTS provider interface (Piper / Edge TTS)
- [ ] Voice end-to-end path

**Acceptance:** "Hey Jarvis, open Google on my laptop" works entirely by voice.

---

## Phase 6 — Memory ⬜

- [ ] Working / session / long-term memory
- [ ] Importance scoring + retention rules
- [ ] Context resolution for follow-ups
- [ ] Episodic memory (optional embeddings later)

**Acceptance:** "Remember that's my bike engine" → "What's my bike engine?" works.

---

## Phase 7 — Web Research ⬜

- [ ] `web.search`, `web.read_page`, content extraction, source URLs
- [ ] Staleness detection + known-vs-uncertain framing
- [ ] Tests with stubbed HTTP

**Acceptance:** JARVIS researches current information without hallucinating.

---

## Phase 8 — Home Assistant ⬜

- [ ] `integrations/home_assistant` (list, read, on/off, brightness, scenes)
- [ ] Entity tools in registry; credentials server-side only

**Acceptance:** "Turn my room light off" executes on the right entity.

---

## Phase 9 — Android Companion ⬜

- [ ] Flutter client: auth, chat, microphone, TTS playback, device status
- [ ] `docs/ANDROID_AGENT.md`

---

## Phase 10 — Advanced Agent ⬜

- [ ] Multi-step planning / task queue / persistent scheduler
- [ ] Event bus + rule engine (proactive behaviour)
- [ ] Long-term memory embeddings
- [ ] Plugin architecture (`plugins/`)
- [ ] Face-recognition auth plugin (from legacy)

---

## Cross-cutting (every phase)

- [x] Tests added alongside functionality
- [x] Docker Compose stays runnable
- [x] Docs updated on architecture changes
- [x] Logically grouped commits
- [ ] Secrets never committed (`.env` only)

---

## Milestones

| Milestone | Target phase | Description |
|---|---|---|
| M1: Brain is alive | 2 | Contextual chat via any supported provider |
| M2: Brain has hands | 3–4 | Tool calling + Windows device control |
| M3: Brain has ears/voice | 5 | Wake-word voice pipeline end-to-end |
| M4: Brain remembers | 6–7 | Persistent personal memory + web research |
| M5: Home/away everywhere | 8–10 | Home Assistant, Android, scheduler, events |
